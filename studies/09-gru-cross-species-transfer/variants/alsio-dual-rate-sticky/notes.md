# Alsiö (rat) dual-rate sticky-RL sensitivity

Fit the winning Alsiö model family—separate win/loss learning rates plus
one-trial spatial stickiness—per rat on the matched odd-session adaptation
half, then score the held-out even sessions.

This is not labeled an author-aligned reproduction: the paper selected the
model on cohort VI, while the public sessions admitted under schema v1 are
cohorts II–V. It is retained as an author-model sensitivity for the descriptive
cohort. CPU-only Allen HPC job; never submit to Beaker.

Tracking: dispatcher #162.
